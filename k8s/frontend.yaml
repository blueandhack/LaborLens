apiVersion: apps/v1
kind: Deployment
metadata:
  name: laborlens-frontend
  namespace: laborlens
spec:
  replicas: 1
  revisionHistoryLimit: 3
  selector:
    matchLabels:
      app: laborlens-frontend
  template:
    metadata:
      labels:
        app: laborlens-frontend
    spec:
      containers:
      - name: frontend
        image: ghcr.io/blueandhack/laborlens-frontend:f858efdf8ef69c11b6ecf03ca0e8ac4fb35fe53f
        imagePullPolicy: Always
        ports:
        - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: laborlens-frontend
  namespace: laborlens
spec:
  selector:
    app: laborlens-frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
