apiVersion: apps/v1
kind: Deployment
metadata:
  name: pwd-frontend
  namespace: pwd-lookup
spec:
  replicas: 1
  selector:
    matchLabels:
      app: pwd-frontend
  template:
    metadata:
      labels:
        app: pwd-frontend
    spec:
      containers:
      - name: frontend
        image: ghcr.io/blueandhack/laborlens-frontend:76845a566d2dd43b556b4ab7a94bde328451e934
        imagePullPolicy: Always
        ports:
        - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: pwd-frontend
  namespace: pwd-lookup
spec:
  selector:
    app: pwd-frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
