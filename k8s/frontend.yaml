apiVersion: apps/v1
kind: Deployment
metadata:
  name: laborlens-frontend
  namespace: laborlens
spec:
  replicas: 1
  revisionHistoryLimit: 3
  selector:
    matchLabels:
      app: laborlens-frontend
  template:
    metadata:
      labels:
        app: laborlens-frontend
    spec:
      containers:
      - name: frontend
        image: ghcr.io/blueandhack/laborlens-frontend:6a7bca2069c3ed64038a69113b919395c2f9dc2d
        imagePullPolicy: Always
        ports:
        - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: laborlens-frontend
  namespace: laborlens
spec:
  selector:
    app: laborlens-frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
