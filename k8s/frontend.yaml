apiVersion: apps/v1
kind: Deployment
metadata:
  name: pwd-frontend
  namespace: pwd-lookup
spec:
  replicas: 1
  selector:
    matchLabels:
      app: pwd-frontend
  template:
    metadata:
      labels:
        app: pwd-frontend
    spec:
      containers:
      - name: frontend
        image: ghcr.io/blueandhack/laborlens-frontend:05a4f91035e8350da89c4efba4ce70179486600e
        imagePullPolicy: Always
        ports:
        - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: pwd-frontend
  namespace: pwd-lookup
spec:
  selector:
    app: pwd-frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
